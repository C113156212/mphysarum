這個程式主要是要自動化環境建置，所以我想，應該要可以客製化一些東西，像是：
url, acc, pwd
這樣子，讓使用者可以自己輸入想要的參數，然後自動化建置環境。
可是實際上在操作時，不同網頁的輸入欄位名稱都不一樣
像是有的叫 username, password 有的叫 user, pass
有的甚至還會有 email, phone 之類的，這樣子就很難統一處理。
所以我想了一個辦法，就是讓使用者可以自己定義一個 mapping 檔案


所以我應該設定成像字典那樣 --> "指令": "參數"
{
  "catch": "mat-button-wrapper",
  "accin": "account12345",
  "pwdin": "qwert12345"
}
程式則照著這個指令陣列去執行對應的舉動。
例如catch --> 找到對應的 class name
accin --> 找到對應的輸入欄位，然後輸入帳號
pwdin --> 找到對應的輸入欄位，然後輸入密碼
submit --> 找到對應的按鈕，然後點擊
這樣就可以達到客製化的效果了。